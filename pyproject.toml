[tool.ruff]
target-version = "py311"
line-length = 110
exclude = ["data", ".venv", "__pycache__"]

[tool.ruff.lint]
# Default rule set (E, F) is enough here — not adding stricter categories
# like import-sorting or docstring rules for a project this size.
select = ["E", "F"]
